<div class="navbar-wagon navbar-fixed-top">
  <!-- Logo -->
  <%= link_to root_path, class: "navbar-wagon-brand" do %>

  <%= image_tag "logo.svg", height: 35, id: "logo"%>

  <% end %>
<!--   <div class="dropdown">
  <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown">
     <i class="fe fe-calendar"></i>
  </button>
  <div class="dropdown-menu">
    <a class="dropdown-item" href="#">Dropdown link</a>
    <a class="dropdown-item" href="#">Dropdown link</a>
  </div>
</div>
-->
<!-- Right Navigation -->
<div class="navbar-wagon-right hidden-xs hidden-sm">

  <% if user_signed_in? %>

  <!-- Links when logged in -->
  <% if current_user.first_name.present? && current_user.last_name.present? %>
    <%= link_to "List a Space", new_space_path, class: "btn btn-sm btn-outline-primary margin-right-15" %>
  <% else %>
    <%= link_to "List a Space", edit_profile_path(current_user), class: "btn btn-sm btn-outline-primary margin-right-15" %>
  <% end %>
  <%= link_to "Profile", user_path(current_user), class: "btn btn-sm btn-outline-primary margin-right-15" %>
  <%= link_to "Favorites", favorites_path, class: "btn btn-sm btn-outline-primary margin-right-15" %>


  <!-- Links when logged in -->
  <!-- Avatar with dropdown menu -->
  <div class="navbar-wagon-item">
    <div class="dropdown">
      <% if current_user.avatar.present? %>
        <%= cl_image_tag current_user.avatar, class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
        <% else %>
        <%= image_tag "https://randomuser.me/api/portraits/women/64.jpg", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
        <% end %>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li>
          <%= link_to user_path(current_user) do %>
          <i class="fa fa-user"></i> <%= t(".profile", default: "Profile") %>
          <% end %>
        </li>
        <li>
          <%= link_to spaces_path do %>
          <i class="fa fa-home"></i>  <%= t(".spaces") %>
          <% end %>
        </li>
        <li>
          <%= link_to destroy_user_session_path, method: :delete do %>
          <i class="fa fa-sign-out"></i>  <%= t(".sign_out") %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
  <% else %>
  <!-- Login link (when logged out) -->
  <!-- Login link (when logged out) -->
  <%= link_to t(".sign_in", default: "Log In"), new_user_session_path, class: "btn btn-sm btn-outline-primary" %>
  <% end %>
</div>

<!-- Dropdown list appearing on mobile only -->
</div>


<br>
<div class="container" style="width: 30%">

  <h1 class="text-center">Review your stay at <br><br>"<%= link_to @booking.space.name, space_path(@booking.space) %>"</h1>

  <div class="wrapper-grey padded">
   <!--      <div class="container"> -->
    <%= simple_form_for([@booking, @review]) do |f| %>
    <!-- [...] -->
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.button :submit, :class => 'btn btn-primary text-center' %>
    <!-- [...] -->
    <% end %>
  </div>
</div>

